const button = document.querySelector("#helloBtn");
const message = document.querySelector("#message");

button.addEventListener("click", () => {
  message.textContent = "Keep coding. Keep building. 🚀";
});
